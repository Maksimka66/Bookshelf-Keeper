// import { RootState } from '@/store/store';

// export default function setAuthHeader(headers: Headers, { getState }) {
//     const state = getState() as RootState;
//     const token = state.auth.token;

//     if (token) {
//         headers.set('Authorization', `Bearer ${token}`);
//     }

//     return headers;
// }
